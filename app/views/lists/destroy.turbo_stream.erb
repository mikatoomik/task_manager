<% if @list.destroyed? %>
  <%= turbo_stream.remove "list_card_#{@list.id}" %>
<% else %>
  <%= turbo_stream.replace "list_card_#{@list.id}", partial: "lists/card", locals: { list: @list } %>
<% end %>
